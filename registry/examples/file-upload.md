## Button-triggered

```tsx
<FileUpload label="Choose file" onChange={setFiles} />
```

## Dropzone

Drag-and-drop target with a click-to-browse fallback.

```tsx
<Dropzone accept="image/*" multiple onChange={setFiles} />
```
