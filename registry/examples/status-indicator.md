## Basic

```tsx
<StatusIndicator variant="success" label="Online" pulse />
```

## All variants

Each StatusTone pairs a color with a required label, so status is never color-only.

```tsx
<StatusIndicator variant="warning" label="Pending" />
```
