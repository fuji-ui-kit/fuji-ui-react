## Basic

No syntax highlighting by design, so the package ships no highlighter.

```tsx
<CodeBlock language="tsx" code={`<Button>Save</Button>`} />
```

## Collapsible

Starts collapsed, for a long snippet that is not the point of the page.

```tsx
<CodeBlock collapsible language="bash" code="npm install @fujiui/react" />
```
