## Basic

Compact by default; pass fullWidth to stretch it.

```tsx
<FormField name="quantity">
  <FormField.Label>Quantity</FormField.Label>
  <NumberInput defaultValue={1} min={1} max={99} />
</FormField>
```
