# Externally managed content, not authored or maintained by this repo -
# reformatting it would just get overwritten (or drift back out of format) on
# the next `npx skills add` install, and it isn't ours to reflow anyway.
/.agents/

# build output
/dist
/mcp/dist
/coverage
/storybook-static
/.storybook/generated

# Agent worktrees are checked out inside the repo, so they would otherwise be
# formatted (and reported on) as part of this branch. Each worktree formats
# itself on its own branch.
/.claude/worktrees/
